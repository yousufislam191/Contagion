import 'package:flutter/material.dart';
class IslamicStudies extends StatelessWidget {
  const IslamicStudies({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Islam"),
      ),
      body: Center(
        child: Text("This is a My Course Page"),
      ),
    );
  }
}